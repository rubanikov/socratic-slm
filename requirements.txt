openai>=1.40.0
python-dotenv>=1.0.0
tqdm>=4.66.0
pandas>=2.2.0

# training stack - pinned to the versions the QLoRA run is verified against
# (train_qlora.py relies on TRL 1.10 behavior: SFTConfig max_length,
# assistant_only_loss auto-patching Qwen3's chat template)
torch==2.13.0
transformers==5.15.0
trl==1.10.0
peft==0.20.0
bitsandbytes==0.50.1
datasets==5.0.1
accelerate==1.14.0
trackio==0.35.0
